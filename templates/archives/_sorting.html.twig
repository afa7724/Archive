{# On utilise le knp paginator bundle package dependance  #}
{{ knp_pagination_sortable(archives,'Trie par date promotion ','a.datepromotionOn')}}
{# {{ knp_pagination_sortable(dechets,'Trie par Promo','d.promo')}}
   #}
